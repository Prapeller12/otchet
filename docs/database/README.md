# База данных

Рабочая схема ещё не создана. До утверждения справочников и эталонных форм нельзя фиксировать таблицы и связи на основе предположений.

Подтверждено: SQLite применяется для локального MVP, PostgreSQL — для централизованного режима; количества не хранятся как `float`; проведённые операции не удаляются; изменения схемы выполняются только последовательными миграциями.
